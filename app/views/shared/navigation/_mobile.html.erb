<div x-description="Mobile menu, toggle classes based on menu state." x-state:on="Open" x-state:off="closed" :class="{ 'block': open, 'hidden': !open }" class="border-b border-gray-700 md:hidden hidden">
    <%= render "shared/navigation/menu_links_mobile" if current_user %>
    <%= render "shared/navigation/profile_links_mobile" if current_user %>
</div>