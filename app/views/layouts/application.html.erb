<!DOCTYPE html>
<html>
  <%= render "shared/head/head" %>
  <body class="bg-gray-200">
    <div>
      <%= render "shared/header/flash" %>
      <%= render "shared/header/header" %>
      <main class="-mt-32">
        <div class="max-w-7xl mx-auto pb-12 px-4 sm:px-6 lg:px-8">
          <div class="bg-white shadow px-5 py-6 sm:px-6">
              <%= yield %>
              <%= render "shared/misc/debug" if Rails.env == 'development' %>
          </div>
        </div>
      </main>
    </div>
  </body>
</html>